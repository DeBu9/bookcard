# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-09-16

### 变更

- 改用工程专属 keystore 正式签名，关闭调试标志（`application-debuggable` 消失），体积 3.17MB → 2.4MB
- 签名凭据从 `build.gradle` 移到 `android/keystore.properties`（不进仓库），仓库内只留 `.example` 模板
- 新增 Gradle Wrapper，`build-apk.sh` 改为自动探测 JDK / Android SDK / Gradle，不再依赖特定机器的路径

### 注意

- 签名与 1.0.0 不同，**无法覆盖安装**。升级前先在旧版「我的 → 导出 JSON」，卸载后装新版再导入。

## [1.0.0] - 2026-09-16

首个正式版本。相比早期的 `我的读书卡 v3.x`，包名由 `com.ruyi.bookcard` 改为 `com.debugmh.bookcard`（对安卓而言是全新应用，可与旧版共存，但数据不互通）。

### 新增

- 记录读书卡：书名 / 作者 / 章节 / 摘录 / 想法 / 标签 / 封面
- 拍照或截图 OCR 识别文字（Tesseract.js，中文 + 英文）
- 分享卡片图到社交平台、保存到相册；也可分享纯文字
- JSON / TXT 双向导入导出（JSON 为完整备份）
- 「我的」内的读书卡、读过的书可点进二级页查看
- 摘录支持删除、修改（原地替换，`id` 与原始日期不变）
- 保存按钮并入吸顶品牌栏，与标题同一水平线，表单再长也不用回滚

### 变更

- 移除「值得再读」「再遇见」入口
- 移除默认摘录与「恢复示例数据」
- 移除底部导航中的「我的书」，改由「我的」进入

### 修复

- `恢复示例数据` 会污染原始数组（`data` 与 `samples` 同引用）
- TXT 导入遇块内空行会拆散记录，改用 `---` 分隔线切块并兼容无分隔线文件
- 相册目录由中文名改为 `BookCards`，规避部分机型的编码问题
