# 读书卡 v1.0.1 · 打包与改动说明

打包日期：2026-09-16

---

## 一、本次改动（v1.0.1）

| 你的要求 | 落地情况 |
|---|---|
| 出个修复后的包 | 已出。改用**正式 keystore 签名**并关闭调试标志，产出 release 包 `读书卡-v1.0.1.apk`。目的是摘掉「debug 包特征」这个尚未被排除的变量。**说清楚：这只排除了一个变量，不能保证那句小米提示一定消失** —— 若仍出现，就说明确实是该机型对非商店来源应用的固定策略，与包无关（详见「八、已知限制」） |

### v1.0.1：换正式签名出包（本次改动）

上一版是 debug 包（`debuggable=true` + Android 通用调试证书）。小米那句「已拒绝此应用获取敏感权限」虽然是安装器文案、与应用权限无关（本包只有 `INTERNET` 一项，是普通权限），但**迄今出现过的包清一色都是 debug 包**，所以「debug 特征是否触发它」并不能从已有现象里排除。这一版就把这个变量摘掉：

| 项 | 上一版（1.0.0） | 本版（1.0.1） |
|---|---|---|
| 文件名 | `读书卡.apk` | `读书卡-v1.0.1.apk` |
| 版本 | 1.0.0（versionCode 1） | **1.0.1（versionCode 2）** |
| 调试标志 | `debuggable=true` | **已关闭** |
| 签名 | `CN=Android Debug`（SDK 通用调试证书） | **`CN=BookCard`（本工程专属 keystore）** |
| 体积 | 3,173,490 字节 | **2.4MB**（去掉调试信息） |
| 用户权限 | 只有 `INTERNET` | 只有 `INTERNET`（未变） |

出包与校验结果：

| 项 | 值 |
|---|---|
| keystore | `android/keystore/bookcard.keystore`（别名 `bookcard`，RSA 2048，有效期 10000 天） |
| 证书 SHA-256 | `8c4d9e40564623a35056dee3397df3d8870439c90ac1eac1b8f32faae3dff159` |
| 包 MD5 | `7c3a36d61763e26f14a25df54b2764ac` |
| 包 SHA256 | `4f99f9e520faa545d1d33dbd8bca1305b2bc9dca8a07f55cc970e33fbc636f7b` |
| zipalign | `Verification succesful` |
| 包内页面 | 与源文件 md5 一致（`2ca56353…`），确认打进包里的是最新页面 |
| 权限 | 仍只有 `INTERNET`（1 项） |

构建命令也跟着升级了，以后一条命令出正式包：

```bash
bash build-apk.sh            # 正式签名包（默认）
bash build-apk.sh debug      # 带调试标志的包，仅本机排查用
```

#### ⚠️ 签名换了 → 装之前必须先卸载旧版

新旧两版**包名相同、签名不同**，安卓不允许覆盖安装，会直接报「应用未安装」或「与已有应用签名不一致」。

正确顺序：

1. 打开手机上已装的「读书卡」→「我的 → 导出 JSON」（**这步不能省，卸载会清空数据**）
2. 卸载旧版
3. 安装 `读书卡-v1.0.1.apk`
4. 打开新版 →「我的 → 导入 JSON」，数据回来

#### keystore 必须长期保存

`android/keystore/bookcard.keystore` 连同它的密码（别名 `bookcard`）要另外备份一份（微盘、网盘都行）。

- **丢了 = 以后所有更新都装不上**，只能让用户卸载重装、数据清空
- 密码写在 `android/keystore.properties`（已 gitignore，不进仓库），`build.gradle` 只负责读取
- 该文件目前不在版本控制里，别人 clone 后需自行创建：复制 `keystore.properties.example` 改名、填自己的 keystore 路径与密码，或改用 debug 包

### v1.0.0：上一版的改动

| 你的要求 | 落地情况 |
|---|---|
| 包名改为 `com.debugmh.bookcard` | 已改。**包名不是单点配置**，联动修改了 4 处：构建配置的 `namespace` 与 `applicationId`、Kotlin 源码目录结构与 `package` 声明、FileProvider 授权标识（`fileprovider`）、动态权限名 |
| 版本号改为 1.0.0 | 已改（`versionName "1.0.0"` / `versionCode 1`） |
| 名称改为「读书卡」 | 已改。桌面图标名、网页标题、首页大标题、卡片图上的品牌水印、导出文件默认名（`读书卡-日期.json`）全部统一 |
| 保存按钮移到顶部浮动 | 已改。原来在表单最底部，要填完滚到底才能点；现在滚动到任何位置都能点。见下方「保存按钮与品牌栏平行」 |
| 保存按钮与读书卡平行 | 已改。上一版按钮单独占掉一整行、把「记录一张读书卡」标题挤到下一行；现在并进品牌栏，与「读书卡」大标题**同一水平线**（标题在左、按钮在右），省掉一整行高度 |

### 保存按钮与品牌栏平行（本次改动）

- 位置：**住在品牌栏里**，和「读书卡」大标题并排同一行，按钮靠右
- 因为品牌栏本身是吸顶的，按钮自然跟着吸顶 —— 滚到表单任何位置，按钮都还在右上角，不必回滚到底部
- 只在**记录页**出现，由 `show()` 写入的 `body[data-view="add"]` 控制显隐；首页与「我的」页品牌栏保持原样，只有一个标题
- 按钮组内容不变：右侧「保存这张读书卡」（编辑态变「保存修改」），左侧「取消修改」（仅编辑态出现）
- 上一版用来把浮动条钉在品牌栏下方的 `--toph`（品牌栏实测高度）**已连同 `measureTopbar()` 一并移除** —— 按钮既然住进品牌栏，就不需要再算这个偏移量了，少一个随屏幕旋转/尺寸变化重算的地方
- 窄屏（≤400px）下按钮内边距与字号自动收一档，保证「读书卡」标题不被挤换行

> 进入记录页时按钮就在右上角，表单再长也不用回滚。

### ⚠️ 这次要先卸载：两版之间签名不一样

分两种情况，别混：

**① 对上一版 1.0.0（同一个包名 `com.debugmh.bookcard`）** —— 包名相同、**签名不同**，安卓不允许覆盖安装，会直接报「应用未安装 / 与已有应用签名不一致」。所以必须按顺序来：

1. 旧版「我的 → 导出 JSON」（**不能省，卸载会清空数据**）
2. 卸载旧版
3. 安装 `读书卡-v1.0.1.apk`
4. 新版「我的 → 导入 JSON」，数据回来

**② 对更早的「我的读书卡」v3.x（旧包名 `com.ruyi.bookcard`）** —— 包名不同，安卓视为两个互不相干的应用：

- **可以同时安装**，不会互相覆盖
- **数据完全隔离**：新版读不到旧版记录的读书卡
- 想搬数据：旧版「我的 → 导出 JSON」→ 新版「我的 → 导入 JSON」

---

## 二、交付物

| 文件 / 目录 | 说明 |
|---|---|
| `读书卡-v1.0.1.apk` | **可直接安装的正式签名包（本次交付）** |
| `index.html` | 网页版本体，浏览器直接打开即可用；同时也是 APK 内实际加载的页面 |
| `index.original.html` | **你最初的 index.html 备份**，未做任何改动，可随时回滚 |
| `android/` | Android 工程源码（Kotlin + WebView 外壳） |
| `android/keystore/bookcard.keystore` | **正式签名文件，必须长期保存**（丢了以后就无法更新） |
| `build-apk.sh` | 一键重建脚本（自动同步 assets → 编译 → 复制产物）；默认出正式包 |
| `smoke-test.js` | 自动化冒烟测试，桥接模式 **92 项**、浏览器模式 **83 项**断言，当前全部通过 |
| `README.md` | 项目说明（已更新为 v1.0.1 的构建与测试命令） |

### 应用信息

| 项 | 值 |
|---|---|
| 应用名 | 读书卡 |
| 包名 | `com.debugmh.bookcard` |
| 版本 | 1.0.1（versionCode 2） |
| 签名 | `CN=BookCard`（正式 keystore，非调试证书） |
| 支持系统 | Android 8.0 及以上（minSdk 26 / targetSdk 34） |
| 图标 | 自适应图标（纯 XML 矢量，无需 PNG 资源） |

### 安装

传到手机 → 点击安装 → 提示时允许「安装未知应用」。

**这次要先卸载旧版**（签名变了，与上一版 1.0.0 不能覆盖安装）—— 卸载前先在旧版里「我的 → 导出 JSON」备份。

> 目录里还留着早先的 `我的读书卡-v3.0/v3.1/v3.2-debug.apk`（旧包名 `com.ruyi.bookcard`，与新版可共存）和上一版的 `读书卡.apk`（同包名，装新版前须卸载）。

> 正式签名是自己生成的 keystore，跨机器重新打包时只要还用同一个 `bookcard.keystore`，就能覆盖安装、不必卸载。

> **小米 / 红米（MIUI、澎湃 OS）安装时会多弹一句「已拒绝此应用获取敏感权限」——可以忽略。**
>
> - 实测（2026-09-16，小米真机）：这句话**与本应用实际申请的权限无关**。本包只声明 `INTERNET` 一项，是普通权限、装机自动授予，包里没有任何敏感权限（可用 `aapt2 dump badging` 复核）。
> - 它是小米安装器对「非应用商店来源应用」的通用拦截文案，**提示之后安装照常完成，功能不受影响**（该机已实测装上并可用）。
> - 之前几个包在同一台机器上也弹同样的话。**v1.0.1 已换成正式签名 + 关闭调试标志**，专门用来排除「debug 包特征触发它」这个可能：若这一版仍弹，即可确认是机型策略，与包无关，不必再做任何包侧改动。
> - 万一某台小米机型真的装不上，按顺序试：① `设置 → 应用设置 → 纯净模式` 关闭；② `设置 → 应用设置 → 授权管理 → 安装未知应用`，给「文件管理 / 浏览器 / 微信」等打开安装包的程序开权限；③ 弹窗里手动选「继续安装」而不是「拒绝」；④ 电脑上用 `adb install -r`，绕过手机安装器。

---

## 三、权限：仍然只有 1 项

`android.permission.INTERNET`（网络访问）。

**明确没有申请**：存储、相机、通讯录、位置、电话、麦克风。本次新增的删除/修改功能纯属本地数据操作，不涉及任何新权限。

为什么必须保留网络权限：OCR 走 `Tesseract.js`，识别引擎与语言包不内置，首次识别时从 CDN 拉取。其余功能全部本地完成：

- 导出 JSON/TXT → Android 10+ 写系统「下载」目录（MediaStore），8/9 写 App 专属目录
- 保存卡片图到相册 → MediaStore 图片集（`Pictures/BookCards/`）
- 分享卡片图 → FileProvider + 系统分享面板（`content://` 授权读取，非公共目录）
- 分享文字 → 系统分享面板

---

## 四、修改与删除怎么用

### 修改

卡片右上角 `⋯` → **修改这张卡**。

表单会预填该书卡的全部字段（书名、作者、章节、摘录、想法、标签、封面）。表单标题变成「修改这张读书卡」，右上角品牌栏里的按钮变成「保存修改」，并多出一个「取消修改」。

保存时是**原地替换**：`id` 不变、**原始记录日期不变**（改的是文字，不是「何时读的」）。总条数不会增加。

改到一半不想改了，点右上角「取消修改」，或按手机返回键，都会丢弃改动回到首页。

### 删除

卡片右上角 `⋯` → **删除这张卡** → 确认。

删除后会从屏幕底部浮出一条**黑色撤销条**（"已删除《书名》　撤销"），**6 秒内点「撤销」可以还原**，并且还原到**原来的排列位置**，不是简单追加到末尾。6 秒后自动消失。

> 撤销窗口只在内存里，关掉应用就没了。删除属于不可逆操作，长期重要的内容建议定期「我的 → 导出 JSON」备份。

---

## 五、导入功能怎么用

### 导入 JSON（推荐，无损）

「我的 → 导入 JSON」→ 选文件。识别到的条数会先弹窗确认，避免误导入。

导出与导入是同一套结构，所以**从任意设备导出的 JSON 都能原样还原**（书名、作者、章节、内容、想法、标签、日期、封面全部带上）。

导入时按「书名 + 内容」判重，**内容完全相同的条目自动跳过**，不会重复堆积。

### 导入 TXT（便于阅读与粘贴）

TXT 没有严格格式要求，识别规则如下：

- **每条之间空一行**（用本应用导出的 TXT 是 `---` 分隔线，优先按它切块）
- 首行建议写 `《书名》｜作者`，中英文竖线都认
- 首行下面的短行会被当作**章节**
- `【想法】…`、`【标签】A、B` 会被分别取出
- 单独的 `2026-09-15`、`2026/9/15` 这类行会被当作日期
- 其余行合并为摘录内容
- **首行不像书名的，整块作为正文**，标题记为「未命名」——宁可没标题也不丢内容

例：

```
《活着》｜余华
第一章

“人是为了活着本身而活着的。”

【想法】答案不在远方。
【标签】人生、生活
2026-09-15

---

《月亮与六便士》｜毛姆

“满地都是六便士，他却抬头看见了月亮。”
```

**编码**：Windows 记事本默认存 ANSI/GBK。程序先按 UTF-8 解码，发现乱码字符会自动改用 GBK 重解，不会出现一屏乱码。

---

## 六、卡片图分享

点卡片右上角 ↗ → 底部弹出分享面板，自动生成 **1080px 宽**的竖版卡片图（高度随内容自适应），并在面板里先给你预览，确认没问题再发。

三种动作：

| 按钮 | 行为 |
|---|---|
| 分享图片卡片 | 拉起系统分享面板，可直接发微信、微博等 |
| 保存到相册 | 存到 `Pictures/BookCards/`，不需要存储权限 |
| 分享纯文字 | 生成「“摘录”——《书名》· 作者」文本，走系统分享面板 |

卡片图上会有：品牌标识、日期、摘录正文（衬线字体）、你的想法（左侧竖线引注）、标签、底部书名与作者。

> 摘录特别长时会按比例整体缩小，避免图片过大导致生成失败。

---

## 七、为什么不只是"套个壳"

直接用 WebView 加载网页，下面这些功能会**静默失效**——点了没反应、不报错，最难排查。这些都已处理：

| 网页的做法 | 在 WebView 里的实际结果 | 处理方式 |
|---|---|---|
| OCR 引擎从 CDN 加载 | 页面是 `file://` 源，跨域 fetch 被 Chromium 拦截，Tesseract 加载 worker/wasm/语言包**全部失败** | 用 `WebViewAssetLoader` 把页面挂到 `https://appassets.androidplatform.net/`，拿到正常 HTTPS 源 |
| 导出用 `<a download>` + blob | WebView 无 `DownloadListener` 时**静默丢弃** | 新增原生桥接 `saveText()`，由原生落盘并提示保存路径 |
| 分享用 `navigator.share` | WebView 里没有这个 API；回退的 `navigator.clipboard` 又依赖安全上下文，**两条路都可能失败** | 新增桥接 `shareText()`（文字）与 `beginImage/pushImagePart/shareImageDone`（图片），拉起系统分享面板 |
| **卡片图 base64 一次性传原生** | 1–3MB 字符串超出 IPC 上限，**被静默丢弃、无异常**，表现为「点了分享没反应」 | 统一按 **64KB 分片**推送，原生侧拼装；缺片时明确报「图片接收不完整」而非静默失败 |
| `alert()` / `confirm()` | 不实现 `WebChromeClient` 回调时**被静默吞掉**：提示不显示、删除确认永远被当成取消 | 原生实现 `onJsAlert` / `onJsConfirm` 代弹对话框 |
| 文件选择（选图/封面/导入文件） | 不实现 `onShowFileChooser` 时**点按钮毫无反应** | 原生拉起系统文件选择器 |
| localStorage | 默认禁用，数据存不住 | `domStorageEnabled = true` |
| 全面屏上下白边 | 白边来自原生窗口层，只改 CSS 无效 | 边到边 + 关闭导航栏对比条纹 + 刘海适配 + 原生注入安全区 CSS 变量 |
| 返回键 | 直接退出应用，弹窗开着也退 | 注入 `__appBack()`：关分享面板 → 关操作面板 → 书架退回「我的」 → 退出编辑态 → 回首页 → 都消费不掉才退出 |

---

## 八、已知限制与风险

1. **删除后只有 6 秒后悔期**
   撤销条过期即无法挽回，且不随应用重启保留。重要内容请定期导出 JSON。

2. **OCR 首次使用必须联网，且要下载约 30MB**
   实测语言包：中文 `chi_sim` 19.2MB + 英文 `eng` 10.4MB（代码用 `chi_sim+eng`，两个都会下）。界面会显示"正在下载语言包"，属正常现象。之后由 WebView HTTP 缓存复用。

3. **签名：v1.0.1 起已换成正式 keystore**
   用自己生成的 `android/keystore/bookcard.keystore` 签名（不再是 Android 调试证书），可以正常分发、也具备上架的形式条件（上架还需另过商店审核）。**这个 keystore 和密码必须长期保存**：同一个包名的后续更新只能用它签名，换一次就得让用户卸载重装、数据清空。

4. **本地存储约 5MB 上限**
   文字数据很小，但封面图是 base64 存在 localStorage。已压缩到最长边 800px 的 JPEG（单张约 100KB 量级），可存数十本。写满时界面会明确提示并**回滚未保存的改动**，不会出现"显示已保存、实际没存"。

5. **导入的 TXT 是启发式解析**
   格式自由是优点也是代价：如果一段话的某一行恰好长得像日期或章节，会被归类成日期/章节。导入前会先弹窗告诉你要导入几条、前三条标题是什么，不对可以取消。

6. **卡片图在真机上的渲染未验证**
   本机没有真机/模拟器，卡片图的字体回退（Android 上衬线中文字体不一定存在，会退到系统默认字体）和分享到微信的实际效果，需要你装到手机上确认。图生成后会在弹层面板里先预览，一眼就能看出效果。

7. **OCR 在真机上的实际效果同样未验证**
   识别失败时界面会直接显示失败原因，把那段文字发我即可定位。

---

## 九、一键重建与测试

改完 `index.html` 后：

```bash
bash build-apk.sh            # 正式签名包（默认）→ 读书卡-v1.0.1.apk
bash build-apk.sh debug      # 带调试标志的包，仅本机排查用
```

脚本会依次：同步 `index.html` → `android/app/src/main/assets/`（这一步容易漏，漏了打出来的就是旧页面）→ 编译 → 复制产物到项目根目录并打印 MD5/SHA256。增量约 30 秒，clean 后首次约 2–4 分钟。

**脚本已支持任意机器**：会自动探测 JDK 17 / Android SDK / Gradle，找不到就去常见安装位置（Android Studio 自带 JBR、各平台 SDK 默认路径）里翻，必要时自动生成 `android/local.properties`。不再依赖某台机器的固定路径。

签名凭据来自 `android/keystore.properties`（已 gitignore），仓库里只留 `.example` 模板；原先硬编码在 `build.gradle` 里的密码已移出。出正式包前脚本会检查该文件是否存在，缺了直接报错退出（避免产出装不上的未签名包）。

跑测试：

```bash
npm install                       # 首次，装 jsdom
npm test                          # 两种模式连跑
# 或单独跑：
node smoke-test.js --with-bridge   # 模拟 APK 环境，验证桥接调用与分片还原（92 项）
node smoke-test.js --no-bridge     # 模拟纯浏览器环境，验证回退路径（83 项）
```

### 构建环境要求

换机器时**不需要**照搬任何固定路径，脚本会自己找。只需要：

| 组件 | 要求 |
|---|---|
| JDK | 17（脚本按 `JAVA_HOME` → 常见安装位 的顺序探测） |
| Android SDK | 含 build-tools 34 + platform 34（按 `ANDROID_HOME` → `~/Android/Sdk` 等探测） |
| Gradle | 8.x（本机已装即可；工程也自带 wrapper 兜底） |
| Node | ≥ 22.22.2（仅跑测试需要） |

> 脚本的 Gradle 查找顺序是「本机已有 Gradle → PATH 里的 gradle → 工程自带 wrapper」。把 wrapper 放最后是有意的：它首次运行要从 `services.gradle.org` 下载约 100MB 发行包，网络不畅时会卡很久。CI 里则由 workflow 直接调用 `./gradlew`，不受影响。
